name: Run
on:
  push:
    branches: [develop]
jobs:
  run:
    runs-on: ubuntu-latest
  steps:
    - run: flutter pub get
    - run: flutter test
    - run: flutter build apk
    - run: flutter build appbundle